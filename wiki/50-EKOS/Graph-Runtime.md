# Graph Runtime

The graph runtime lives under `graph/`.

It provides deterministic graph operations and canonical serialization support used by LOS certification and trust hashing.

Author: Abner Pauneto
