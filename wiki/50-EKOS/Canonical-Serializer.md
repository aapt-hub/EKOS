# Canonical Serializer

The canonical serializer is implemented in `graph/tools/EKOS.CanonicalSerializer.psm1`.

LOS certification and trust evidence modules use canonical JSON and SHA256 hashing to preserve deterministic behavior.

Author: Abner Pauneto
